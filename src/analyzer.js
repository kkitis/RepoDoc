/**
 * Member 2 — TODO regex & line stats.
 * Analyze file contents for TODO matches and line counts.
 * Stub only; implementation TBD.
 *
 * @param {string} filePath
 * @param {string} [source]
 * @returns {import('./types.js').FileStats}
 */
export function analyzeFile(filePath, source = '') {
  void source;
  return { path: filePath, lines: 0, todos: [] };
}
