/**
 * Member 1 — Directory traversal.
 * Walk a tree with Node stdlib (`node:fs`, `node:path`) and collect file paths.
 * Stub only; implementation TBD.
 *
 * @param {string} rootDir
 * @returns {Promise<string[]>}
 */
export async function scanDirectory(rootDir) {
  void rootDir;
  return [];
}
